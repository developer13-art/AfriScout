# End-to-end tests

Full-stack tests that exercise the API with a real database, real Redis,
and mocked or recorded external service responses. They validate that the
entire request lifecycle produces the expected result and side effects.