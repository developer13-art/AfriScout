# Unit tests

Pure-function tests: validators, utils, scoring, deduplication similarity,
transformations, and any logic that does not require a database or Redis.

Use `npm run test:unit` to run only these.